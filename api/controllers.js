const dbConnection = require('./db-connection');

const controllers = {
  method: (req, res) => {
    // use dbConnection
    /*
      mini project in second half of class:
        write queries in these handlers
        everything else works, they just need to write the SQL
    */
  }
};

module.exports = controllers;
